<RelativeLayout xmlns:ads="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/EntireLayoutCategory"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main"
    android:layoutDirection="rtl"
    tools:context=".OtherCategoryFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Modern gold/navy tab header (mirrors Quran/Radio tabs) -->
        <RelativeLayout
            style="@style/Widget.MyStream.TabHeader"
            android:id="@+id/watchtitleCard"
            android:layoutDirection="rtl">

            <!-- Leading avatar (gold-ring app glyph) at start -->
            <de.hdodenhof.circleimageview.CircleImageView
                android:id="@+id/headerAvatar"
                android:layout_width="34dp"
                android:layout_height="34dp"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:layout_marginStart="10dp"
                android:src="@mipmap/ic_launcher_new_transparent9"
                app:civ_border_color="@color/gold_accent"
                app:civ_border_width="1.5dp" />

            <!-- Right-side actions: settings gear + rewards trophy -->
            <LinearLayout
                android:id="@+id/headerActions"
                android:layout_width="wrap_content"
                android:layout_height="?attr/actionBarSize"
                android:layout_alignParentEnd="true"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Settings gear -->
                <ImageButton
                    android:id="@+id/headerSettingsBTN"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/action_settings"
                    android:padding="8dp"
                    app:srcCompat="@drawable/ic_settings_gear"
                    app:tint="@color/gold_accent" />

                <!-- Rewards trophy -->
                <ImageButton
                    android:id="@+id/headerRewardsBTN"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/action_rewards"
                    android:padding="8dp"
                    app:srcCompat="@drawable/outline_emoji_events_24"
                    app:tint="@color/gold_accent" />

            </LinearLayout>

            <!-- Title "المزيد" -->
            <TextView
                style="@style/Widget.MyStream.TabHeader.Title"
                android:id="@+id/recitelisttxt"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:layout_toStartOf="@+id/headerActions"
                android:layout_toEndOf="@+id/headerAvatar"
                android:layout_marginStart="12dp"
                android:text="@string/more_tab_header" />

        </RelativeLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:nestedScrollingEnabled="true"
            android:paddingStart="8dp"
            android:paddingTop="4dp"
            android:paddingEnd="8dp"
            android:paddingBottom="60dp" />

        <com.google.android.gms.ads.AdView
            android:id="@+id/adView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:background="@color/colorPrimary"
            android:visibility="gone"
            ads:adSize="BANNER"
            ads:adUnitId="@string/banner_ad_unit_id" />

    </LinearLayout>
</RelativeLayout>
