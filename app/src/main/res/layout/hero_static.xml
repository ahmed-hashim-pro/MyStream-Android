<?xml version="1.0" encoding="utf-8"?>
<!--
  Generalized navy hero (STATIC / non-collapsing). Include at the top of a vertical layout.
  Configure via HeroController; unused slots are hidden. See hero_collapsing.xml for the
  scroll-collapsing variant. IDs are shared between both hosts so HeroController treats them alike.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/heroRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_navy_hero"
    android:elevation="4dp"
    android:layoutDirection="locale"
    android:orientation="vertical"
    android:paddingBottom="12dp">

    <!-- Row 1: controls (back / spacer / search + action icons) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:paddingStart="6dp"
        android:paddingEnd="6dp">

        <ImageButton
            android:id="@+id/heroBack"
            android:layout_width="wrap_content"
            android:layout_height="?attr/actionBarSize"
            android:layout_alignParentStart="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_back"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:visibility="gone"
            app:srcCompat="@drawable/outline_arrow_back_ios_24"
            app:tint="@color/gold_accent" />

        <LinearLayout
            android:id="@+id/heroActions"
            android:layout_width="wrap_content"
            android:layout_height="?attr/actionBarSize"
            android:layout_alignParentEnd="true"
            android:gravity="center_vertical"
            android:orientation="horizontal" />

        <!-- Centered title used when there's no identity row (variant B). Hidden by default. -->
        <TextView
            android:id="@+id/heroCenterTitle"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:layout_toStartOf="@+id/heroActions"
            android:layout_toEndOf="@+id/heroBack"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:gravity="center"
            android:singleLine="true"
            android:textColor="@color/gold_accent"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="القبلة" />

        <SearchView
            android:id="@+id/heroSearch"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:layout_toStartOf="@+id/heroActions"
            android:layout_toEndOf="@+id/heroBack"
            android:layout_marginStart="6dp"
            android:iconifiedByDefault="true"
            android:queryBackground="@android:color/transparent"
            android:visibility="gone" />
    </RelativeLayout>

    <!-- Row 2: identity (avatar + title + subtitle) + optional action pill. Hidden when unused. -->
    <LinearLayout
        android:id="@+id/heroExpanded"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingEnd="12dp"
        android:visibility="gone">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/heroAvatar"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:src="@mipmap/ic_launcher_new_transparent9"
            android:visibility="gone"
            app:civ_border_color="@color/gold_accent"
            app:civ_border_width="1.5dp" />

        <LinearLayout
            android:id="@+id/heroIdentity"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="8dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/heroTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:singleLine="true"
                android:textColor="@color/text_on_navy"
                android:textSize="18sp"
                android:textStyle="bold"
                tools:text="الأذكار" />

            <TextView
                android:id="@+id/heroSubtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="@color/hint_on_navy"
                android:textSize="11sp"
                android:visibility="gone"
                tools:text="أذكار الصباح والمساء" />
        </LinearLayout>

        <androidx.cardview.widget.CardView
            android:id="@+id/heroActionPill"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layoutDirection="locale"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackground"
            android:visibility="gone"
            app:cardBackgroundColor="@color/transparent"
            app:cardCornerRadius="20dp"
            app:cardElevation="0dp"
            app:cardPreventCornerOverlap="true">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_download_all_button"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:paddingTop="9dp"
                android:paddingBottom="9dp">

                <ImageView
                    android:id="@+id/heroPillIcon"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:contentDescription="@null"
                    app:tint="@color/text_on_gold" />

                <TextView
                    android:id="@+id/heroPillText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="7dp"
                    android:textColor="@color/text_on_gold"
                    android:textSize="13sp"
                    android:textStyle="bold" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>
</LinearLayout>
