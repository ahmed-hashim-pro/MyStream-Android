<?xml version="1.0" encoding="utf-8"?>
<!-- Multi-select box shown in place of the row action disc while the
     contextual bar is up (mockup 16d .cbx): gold fill + dark check when the
     row is selected, muted empty box otherwise. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_activated="true">
        <layer-list>
            <item android:gravity="center">
                <shape android:shape="rectangle">
                    <solid android:color="@color/gold_accent" />
                    <corners android:radius="7dp" />
                    <size
                        android:width="24dp"
                        android:height="24dp" />
                </shape>
            </item>
            <item
                android:drawable="@drawable/ic_check_on_gold"
                android:gravity="center" />
        </layer-list>
    </item>
    <item>
        <layer-list>
            <item android:gravity="center">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <stroke
                        android:width="2dp"
                        android:color="@color/text_secondary" />
                    <corners android:radius="7dp" />
                    <size
                        android:width="24dp"
                        android:height="24dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
