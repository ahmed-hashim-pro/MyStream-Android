<?xml version="1.0" encoding="utf-8"?>
<!-- Dark night-paper chip behind the text-brightness "Preview" sample, so the
     white-alpha preview text stays legible on the light settings card too.
     Fixed dark in both themes — it simulates the night reading page. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#15171C" />
    <corners android:radius="8dp" />
</shape>
