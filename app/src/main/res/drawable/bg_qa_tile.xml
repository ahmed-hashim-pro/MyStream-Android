<?xml version="1.0" encoding="utf-8"?>
<!-- Warm-surface background for Quick Action tiles with gold-faint ripple press state. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/gold_accent_faint">
    <!-- Mask: same shape as the normal background so ripple clips correctly -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <!-- Normal background: white card with a warm hairline, 16dp rounded (A .bn-qcell) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/background_card" />
            <stroke
                android:width="1dp"
                android:color="@color/divider_warm_light" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</ripple>
