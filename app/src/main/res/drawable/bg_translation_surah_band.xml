<?xml version="1.0" encoding="utf-8"?>
<!-- Translation-view surah band: gold-faint rounded box with a gold border,
     matching the mockup .surah-band (gold Amiri text set programmatically). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/gold_accent_faint" />
    <stroke
        android:width="1.5dp"
        android:color="@color/gold_accent_semi" />
    <corners android:radius="12dp" />
</shape>
